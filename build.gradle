apply plugin: 'java'

sourceCompatibility = 1.6
version = '0.1.0-SNAPSHOT'

jar {
    manifest {
        attributes 'Implementation-Title': 'gradle_java_lite_start', 'Implementation-Version': version
    }
}

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'commons-collections', name: 'commons-collections', version: '3.2'
    testCompile group: 'junit', name: 'junit', version: '4.+'
}

test {
    //systemProperties 'property': 'value'
}

uploadArchives {
    repositories {
       flatDir {
           dirs 'repos'
       }
    }
}